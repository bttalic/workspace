package lab5;

public class paliodrom {
public static void main(String[] args) {
	String paliodrom = "Testis";
	String ispis = paliodrom.substring(0, 6);
	char t = 'c';
	Character.toString(t);
	System.out.println(paliodrom.charAt(5) + paliodrom.charAt(4) + paliodrom.charAt(3) + paliodrom.charAt(2) + paliodrom.charAt(1) );
	
}
}
