
	public class textsabiranje {
		
	public static void main(String[] args) {
    int prvi= Integer.parseInt(args[0]);
	int drugi=Integer.parseInt(args[1]);
	int operator = Integer.parseInt(args[2]);
		System.out.println(operator);
	for (int i=0;i<args.length;i++){
		
	}
    
  
 
 
	if (operacija.equals("+"))
		{  System.out.println(brojSpreman + brojSpreman2);  }
	if (operacija.equals("-"))
	{    }
	if (operacija.equals("*"))
	{    }
	if (operacija.equals("/"))
	{    }
	
	
	}
}

	

